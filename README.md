# trimchoice

TOTF2 Gear Multi-Zone Customization — static web prototype.

- **Run locally:** open `index.html` in a browser.
- **GitHub Pages:** enable Pages for this repo; images will work if `assets/gloves.png`, `assets/shorts.png`, and `assets/boots.png` are committed (see `assets/README.md`).